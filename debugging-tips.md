# Debugging Tips

* Something looks wrong visually? → use `@Browser` to let the agent see it
* Describe the bug in plain English — agent reads console logs and identifies the issue
* Persistent bug? → try a different model using the multi-agent feature
