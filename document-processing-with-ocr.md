# Document Processing with OCR

* Use pytesseract for OCR
* Using regex we can extract but it will fail it format varies
* We can create an agent, give it tools(OCR), LLM(gpt-5-mini) and task and ask it to extract the data
* Extraction depends on the quality of OCR
